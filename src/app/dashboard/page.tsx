export default async function DashboardPage() {
  return (
    <div>
      DashboardPage
      {/* <button onClick={() => signOut({callbackUrl: "/"})}>
        Sign out
      </button> */}
    </div>
  )
}
